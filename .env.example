# URL of the FlowState API (server/ folder, deployed on Vercel).
# Copy this file to .env and paste your deployed Vercel URL below (no trailing slash).
# Example: EXPO_PUBLIC_API_URL=https://flowstate-api.vercel.app
# Leave empty to run the app fully offline (no login, data only on the phone).
EXPO_PUBLIC_API_URL=
